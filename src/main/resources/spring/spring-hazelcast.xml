<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:hz="http://www.hazelcast.com/schema/spring"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
          http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
          http://www.hazelcast.com/schema/spring
          http://www.hazelcast.com/schema/spring/hazelcast-spring-3.4.xsd">

    <hz:hazelcast id="instance">
        <hz:config>
            <hz:group name="${hazelcast.group.name}" password="${hazelcast.group.password}"/>
            <hz:network port="${hazelcast.server.port}" port-auto-increment="${hazelcast.server.port.autoincrement}">
                <hz:join>
                    <hz:multicast enabled="false" multicast-group="224.2.2.3"
                                  multicast-port="54327"/>
                    <hz:tcp-ip enabled="true">
                        <hz:members>${hazelcast.server.host}</hz:members>
                    </hz:tcp-ip>
                </hz:join>
            </hz:network>

            <hz:map name="logs" max-size="500" time-to-live-seconds="1800" eviction-policy="LFU"/>

            <hz:serialization>
                <hz:serializers>
                    <hz:serializer type-class="org.mockenize.model.LogBean" implementation="logBeanSerializer"/>
                    <hz:serializer type-class="org.mockenize.model.MockBean" implementation="mockBeanSerializer"/>
                </hz:serializers>
            </hz:serialization>
        </hz:config>
    </hz:hazelcast>
</beans>